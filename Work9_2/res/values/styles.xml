<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    
    
    
    <!-- 黑色的样式. -->
   <!--  <style name="AppTheme2" parent="@android:style/Theme.Black">
        Customize your theme here.
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style> -->

    <style name="wirteTheme" parent="@android:style/Theme.Light"></style>

    <style 
        name="cur_style"
        >
        <item name="android:paddingTop">20dp</item>
        
    </style>
    <!-- 创建一个样式 -->
    <style name="btn_Style">
        <item name="android:textColor">
            #FF0000
        </item>
        <item name="android:background">
            #0000ff
        </item>
        <item name="android:textSize">
            20sp
        </item>
        <item name="android:padding">
            5dp
        </item>
        <item name="android:layout_margin">
            2dp
        </item>
    </style>

  <!--   <style name="DarkTheme" parent="Theme.AppCompat.NoActionBar" >
        <item name="android:colorForeground">@android:color/</item>
    </style> -->
</resources>
